/**
 * @file
 * CRAIN Citybook behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.crainCitybook = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
